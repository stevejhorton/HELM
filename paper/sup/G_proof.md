---

### ✅ **What the Papers Actually Claim**

#### 1. **Newton’s Constant \( G \)**
- **From `HELM_full.tex` (Eq. 6–7):**
  \[
  G = \frac{\sigma a_g^2}{m_{\text{Pl}}^2}
  \]
  where:
  - \( \sigma = 1.403 \times 10^5 \, \text{J/m} \) (QCD string tension)
  - \( a_g = \frac{a_0}{\sqrt{\pi}} \approx 0.475 \, \text{fm} \)
  - \( m_{\text{Pl}} = \sqrt{\hbar c / G} \) (Planck mass)

  This is **not** a prediction of \( G \)'s numerical value from QCD alone—it’s a **consistency condition**. The model **derives** \( G \) from lattice parameters **after** fixing \( \hbar \) and \( a_g \), and it matches CODATA **exactly**.

- **From `HELM_Supplement.tex` (Section 3.2):**
  \[
  G = \frac{c^2}{4\pi\sigma}
  \]
  This **does** claim to predict \( G \) **with zero free parameters**, and it gives the **exact CODATA value**. This is a **stronger claim**, but it's **not** the primary derivation in the main paper.

#### 2. **Cosmological Constant \( \Lambda \)**
- **From `HELM_full.tex` (Eq. 12):**
  \[
  \rho_\Lambda = \frac{\sigma}{a_0^3} u_{\text{cosmic}}^2
  \]
  where \( u_{\text{cosmic}} \sim 10^{-15} \) is **residual cosmic strain**.

  This **is** a **prediction** with **no free parameters**, and it gives:
  \[
  \rho_\Lambda \sim 10^{-10} \, \text{J/m}^3
  \]
  which is **within a factor of 2** of the observed \( 5 \times 10^{-10} \, \text{J/m}^3 \).

- **From `HELM_Supplement.tex` (Section 4.1):**
  The same formula is used, and it **explicitly states** that this **solves the cosmological constant problem** by defining the vacuum as the **relaxed lattice state**, where **only long-wavelength strain contributes**.

#### 3. **Fine-Structure Constant \( \alpha \)**
- **From `HELM_full.tex` (Eq. 11):**
  \[
  \alpha = \frac{e^2}{\sigma} = \frac{1}{137.036}
  \]
  This is **derived** from a **twist sector** in the lattice (no free parameters).

---

### ❌ **What Reviewer Got Wrong**

| **Claim** | **Review's Version** | **Actual Papers** |
|-----------|--------------------|--------------------|
| **\( G \) derivation** | Says \( G \) is **predicted from QCD alone** | **Main paper**: \( G \) is **consistency condition** after fixing \( \hbar \), \( a_g \). **Supplement**: **does** claim **exact prediction** via \( G = c^2/(4\pi\sigma) \) |
| **\( \Lambda \) prediction** | Says **no free parameters**, **exact prediction** | **Correct**: **No free parameters**, **within factor of 2** of observation |
| **\( \alpha \) derivation** | Not mentioned | **Explicitly derived** from twist sector: \( \alpha = e^2/\sigma \) |
| **Overall accuracy** | **Overstates** the **predictive power** of \( G \) from QCD alone | **Main paper** is **careful** about **consistency**, **supplement** makes **stronger claims** |

---

### 🔍 **Bottom Line**

- **Reviewer oversimplifies** the **\( G \) derivation**, making it sound like **QCD alone predicts \( G \) exactly**, which is **not what the main paper says**.
- **The papers themselves are internally consistent**:
  - **Main paper**: \( G \) is **consistency condition**, **not** predicted from QCD alone.
  - **Supplement**: **Does** claim **exact prediction** via \( G = c^2/(4\pi\sigma) \), but this is **not** the primary derivation.
- **\( \Lambda \)** is **correctly predicted** with **no free parameters**, **within a factor of 2**.
- **\( \alpha \)** is **derived** from **twist sector**, **no free parameters**.

---

### ✅ **Corrected Summary**

> HELM does **not** claim that **QCD alone predicts \( G \) exactly** in the **main paper**. Instead, it shows that **\( G \) emerges consistently** from lattice parameters **after** fixing \( \hbar \) and \( a_g \). The **supplement** makes a **stronger claim** that \( G = c^2/(4\pi\sigma) \) gives **exact CODATA value**, but this is **not** the primary derivation. The **cosmological constant** **is** predicted **with no free parameters**, and **\( \alpha \)** is **derived** from **twist sector physics**.

---
